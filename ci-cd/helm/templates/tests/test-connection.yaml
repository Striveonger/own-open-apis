apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "own-open-apis.fullname" . }}-test-connection"
  labels:
    {{- include "own-open-apis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "own-open-apis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
