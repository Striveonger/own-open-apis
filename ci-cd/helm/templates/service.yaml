apiVersion: v1
kind: Service
metadata:
  name: {{ include "own-open-apis.fullname" . }}
  labels:
    {{- include "own-open-apis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "own-open-apis.selectorLabels" . | nindent 4 }}
